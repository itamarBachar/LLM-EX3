ErrorRecoveryAgent[@T.I]: {
    S: {
        ERROR_RECOVERY_PROMPT // Instructions to act as an autonomous debugger, returning a fix_command and retry_command.
    }
    
    U: {
        sys.attempted_command[@T.I]
        env.stderr[@T.I]
    }
}